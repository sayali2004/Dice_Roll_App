pipeline {
    agent any

    environment {
        FLUTTER_HOME = "D:\\flutter"
        JAVA_HOME = "C:\\Program Files\\Java\\jdk-20"
        ANDROID_SDK_ROOT = "C:\\Users\\sayali\\AppData\\Local\\Android\\Sdk"
        PATH = "${FLUTTER_HOME}\\bin;${ANDROID_SDK_ROOT}\\cmdline-tools\\latest\\bin;${env.PATH}"
        MY_ENV_VAR = "Custom Value for Flutter Build"
        JAVA_OPTS = "-Dorg.jenkinsci.plugins.durabletask.BourneShellScript.HEARTBEAT_CHECK_INTERVAL=86400"
    }

    stages {
        stage('Checkout') {
            steps {
                git url: 'https://github.com/sayali2004/Dice_Roll_App.git', branch: 'main'
            }
        }

        stage('Flutter Version') {
            steps {
                powershell 'git config --global --add safe.directory D:/flutter'
                powershell 'flutter --version'
            }
        }

        stage('Accept Android Licenses') {
            steps {
                powershell 'yes | flutter doctor --android-licenses'
            }
        }

        stage('Install Dependencies') {
            steps {
                powershell 'flutter pub get'
            }
        }

        stage('Analyze Code') {
            steps {
                powershell 'flutter analyze'
            }
        }

        stage('Run Tests') {
            steps {
                powershell 'flutter test'
            }
        }

        stage('Build APK') {
            steps {
                powershell 'flutter build apk --release'
            }
        }

        stage('Archive APK') {
            steps {
                archiveArtifacts artifacts: 'build\\app\\outputs\\flutter-apk\\app-release.apk', fingerprint: true
            }
        }
    }

    post {
        always {
            echo 'Pipeline execution complete.'
        }
        success {
            echo '🎉 Build succeeded!'
        }
        failure {
            echo '❌ Build failed.'
        }
    }
}
